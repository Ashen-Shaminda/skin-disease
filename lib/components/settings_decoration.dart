import 'package:flutter/material.dart';

class SettingsDecoration extends StatelessWidget {
  const SettingsDecoration({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
